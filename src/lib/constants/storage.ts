export const WELCOME_KEY = "norte_welcome_seen";

/** Modal de push exibido uma vez (até ativar ou dispensar). */
export const PUSH_MODAL_SEEN_KEY = "norte_push_modal_seen";

/** Card de push dispensado — reaparece após 7 dias se ainda não ativou. */
export const PUSH_CARD_DISMISSED_KEY = "norte_push_card_dismissed_at";

export const PUSH_CARD_DISMISS_DAYS = 7;

export type AuthTab = "login" | "register";
